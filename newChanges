const currentQty = Number(stock.current) || 0;
const alternateQty = Number(stock.other) || 0;

const parts = [];

if (currentQty > 0) {
    parts.push(`${currentQty} parts in your current warehouse.`);
}

if (alternateQty > 0) {
    parts.push(`${alternateQty} parts in your alternate warehouse.`);
    parts.push(`These will take upto 14 days of additional processing time.`);
}

const listTooltip = parts.join(' ');
