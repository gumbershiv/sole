<template>

    <div class="slds-p-around_medium">

        <h2 class="slds-text-heading_medium slds-m-bottom_medium">
            Inventory Component Demo
        </h2>

        <!-- BASIC -->
        <div class="slds-box slds-m-bottom_medium">
            <h3 class="slds-text-title_bold slds-m-bottom_small">Basic View</h3>

            <c-b2-b-product-inventory
                inventory={inventoryMock}
                display-type="basic">
            </c-b2-b-product-inventory>
        </div>

        <!-- LIST -->
        <div class="slds-box slds-m-bottom_medium">
            <h3 class="slds-text-title_bold slds-m-bottom_small">List View</h3>

            <c-b2-b-product-inventory
                inventory={inventoryMock}
                display-type="list">
            </c-b2-b-product-inventory>
        </div>

        <!-- PURCHASE -->
        <div class="slds-box">
            <h3 class="slds-text-title_bold slds-m-bottom_small">Purchase View</h3>

            <c-b2-b-product-inventory
                inventory={inventoryMock}
                display-type="purchase">
            </c-b2-b-product-inventory>
        </div>

    </div>

</template>


import { LightningElement } from 'lwc';

export default class B2bInventoryDemo extends LightningElement {

    // ✅ Mock inventory (aligned with your structure)
    inventoryMock = {
        status: "In Stock",
        otherWarehouseStock: "In Stock",
        purchaseLimit: "99999",
        productSku: "1010500",
        productId: "",
        otherWarehouseInventory: "15",
        globalWarehouseInventory: "21",
        currentWarehouseLeadTime: null,
        currentWarehouseInventory: "6",
        currentWarehouseCode: "UDQS10",
        allowBackOrder: true
    };

    // Optional: if you want to loop instead of hardcoding
    displayTypes = ['basic', 'list', 'purchase'];
}